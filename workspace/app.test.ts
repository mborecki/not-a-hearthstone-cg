import App from './app';

describe('Test', () => {
    it('should works!', () => {
        expect(true).toEqual(true);
    });
});

describe('App', () => {
    it('should exists', () => {
        expect(App).toBeDefined();
    });
})
